import styled from '@emotion/styled';

export const Button = styled.button({
  padding: '5px 10px',
  borderRadius: 5,
  outline: 'none',
  cursor: 'pointer',
  ':hover': {
    backgroundColor: 'lightgrey'
  }
});
